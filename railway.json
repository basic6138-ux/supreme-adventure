{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd Apartment_BoookingSystem/laravel_app && composer install --no-interaction --prefer-dist --optimize-autoloader --no-dev && npm ci && npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "healthcheckPath": "/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "startCommand": "cd Apartment_BoookingSystem/laravel_app && bash scripts/railway-start.sh"
  }
}
